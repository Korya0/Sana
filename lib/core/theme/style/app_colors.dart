import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Core backgrounds and brand colors (Dark Mode Default)
  static const Color scaffoldBackground = Color(0xFF000000);
  static const Color secondaryBackground = Color(0xFF1c1c1e);
  static const Color primary = Color(0xFFD4AF37); // Gold
  static const Color secondry = Color(0xFF2D6A4F); // Green
  static const Color red = Color(0xFFEF5350);

  // Dynamic text and icons in dark mode
  static const Color textPrimary = Color(0xFFFFFFFF);     // White text
  static const Color textSecondary = Color(0xFF81868c);   // Grey text
  static const Color textAccent = Color(0xFFD4AF37);      // Gold text
  static const Color iconAccent = Color(0xFFD4AF37);      // Gold icon
  static const Color facebookBlue = Color(0xFF1877F2);
}
