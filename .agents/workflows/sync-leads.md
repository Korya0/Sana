---
description: Phase 3 - Tech Lead alignment, deep explanation, and git operations.
---

1. Inform the user that the "Missions Synchronizer" is starting.
2. **Deep Technical Justification**:
   - Explain the "WHY" behind non-obvious architecture decisions.
   - Justify the logic as a Tech Lead to a Peer Lead.
3. **Line-by-Line Brain Dump**:
   - Generate `tmp/CODE_EXPLANATION.md` with:
     - Line index.
     - Why this way?
     - Trade-off made at this specific line.
4. **Git Operations**:
   - Check `git status`.
   - Provide the specific `git add .` and `git commit -m "[message]"` command following conventional commits.
   - Provide the `git push origin [branch]` command.
5. Wait for the user to read the explanation before they delete the temporary file.
