# تقرير المخالفات المعمارية - المرحلة الأولى (core/constants, core/utils, core/error)

هذا الملف يحتوي على حصر للمخالفات المعمارية التي تم اكتشافها بناءً على معايير `ARCHITECTURE_RUBRIC.md`. كل مخالفة مرفق معها شرح لكيفية كسرها للمبدأ المعماري، والحل الأفضل المقترح.

## 1. مخالفة مبدأ الانفتاح/الانغلاق (Open/Closed Principle)
**الملف:** `lib/core/constants/religious_event_display_names.dart`

**كيف تم كسر المبدأ؟**
يعتمد هذا الكلاس على استخدام `switch statement` للتحقق من نصوص صريحة (Strings). مبدأ الانفتاح/الانغلاق (أحد مبادئ SOLID) ينص على أن الكود يجب أن يكون "مفتوحاً للإضافة، ومغلقاً للتعديل" (Open for extension, closed for modification). 
في الوضع الحالي، إذا أردنا إضافة أو إزالة أو تعديل مناسبة دينية جديدة، سنضطر إلى الدخول لهذا الملف وتعديل الدالة `getName` بإضافة `case` جديد، مما يكسر المبدأ ويجعل الكود غير قابل للتوسع بشكل نظيف.

**الحل الأفضل:**
- **الخيار الأول (الاعتماد على Enums):** إنشاء `enum` يمثل المناسبات الدينية، وعمل `extension` عليه يرجع الاسم الصحيح من `AppStrings`.
- **الخيار الثاني (الاعتماد على Maps):** تعريف خريطة ثابتة `Map<String, String>` لربط المفاتيح بالنصوص مباشرة، بحيث يتم الإرجاع عن طريق الـ `Key` دون الحاجة لاستخدام جمل شرطية.

---

## 2. مخالفة فصل الإعدادات والبيانات الحساسة (Configuration & Environment Separation)
**الملف:** `lib/core/constants/app_constants.dart`

**كيف تم كسر المبدأ؟**
تم وضع الرمز السري `adminSecretPin` كقيمة ثابتة (Hardcoded) ومحفوظة صراحةً داخل الكود. المبدأ (في Module 12) يقتضي فصل البيانات الحساسة أو إعدادات البيئة (مثل الأرقام السرية، مفاتيح الـ API) عن الكود المصدري لسهولة تغييرها لاحقاً دون الحاجة لرفع تحديث جديد للتطبيق، وللحفاظ على الأمان. (الـ `TODO` الموجود في الكود يوضح إدراك المطور لهذه المشكلة).

**الحل الأفضل:**
نقل هذه القيمة لتُدار عبر `Firebase Remote Config`، وعند بدء تشغيل التطبيق يتم سحب القيمة وحفظها محلياً في المتغيرات، أو استخدام بيئة `.env` إذا كان الأمر يتطلب أماناً أعلى.

---

## 3. غياب استراتيجية التصدير لتنظيف الاستيرادات (Barrel Files & Export Strategy)
**المجلدات:** `lib/core/constants`، `lib/core/utils`، `lib/core/error`

**كيف تم كسر المبدأ؟**
لا يوجد ملفات تجميعية (مثل `index.dart` أو `constants.dart`) تقوم بعمل `export` للملفات المتعددة بداخل كل مجلد. هذا يؤدي إلى كثرة وتشتت جمل الـ `import` في الملفات الأخرى التي تستخدم هذه الأدوات، مما يتعارض مع (Module 6) في الروبيرك الذي ينص صراحةً على استخدام الـ Export files لتنظيف واختصار الاستيرادات.

**الحل الأفضل:**
إنشاء ملف يجمع كل المكونات، مثلاً `constants.dart` داخل `lib/core/constants` يحتوي على:
```dart
export 'api_endpoints.dart';
export 'app_assets.dart';
export 'app_constants.dart';
export 'app_links.dart';
export 'app_strings.dart';
```
بحيث يستورد أي ملف خارجي ملفاً واحداً فقط. وتطبيق ذلك على باقي المجلدات.

---

## 4. كسر مبدأ المسؤولية الواحدة والأرقام السحرية (SRP / Magic Numbers)
**الملف:** `lib/core/utils/context_extension.dart`

**كيف تم كسر المبدأ؟**
ملف `context_extension` وظيفته توفير وصول سريع لخصائص `BuildContext` (مثل الألوان أو الثيم). لكنه حالياً يحتوي على دالة `responsive(num size)` التي تستخدم رقماً سحرياً `375` (عرض التصميم الافتراضي) وتجري حسابات التحجيم. هذا يدمج مسؤولية "التصميم التجاوبي (Responsive UI)" داخل إضافة عامة للـ Context، وهو ما يكسر مبدأ المسؤولية الواحدة (SRP). كما أن استخدام الأرقام السحرية يضعف مقروئية الكود.

**الحل الأفضل:**
إنشاء كلاس منفصل ومخصص (مثل `lib/core/utils/responsive_utils.dart` أو استخدام باقة مثل `flutter_screenutil`) ليكون مسؤولاً حصرياً عن حسابات الشاشات، وإبقاء الـ Context Extension نظيفاً للوصول السريع فقط.

---

## 5. تواجد بيانات وهمية تضر بقابلية الصيانة (Maintainability)
**الملف:** `lib/core/constants/app_links.dart`

**كيف تم كسر المبدأ؟**
قيمة رابط متجر آبل (`appStore`) تحتوي على القيمة الوهمية `idXXXXXXXXXX`. ترك بيانات وهمية (Dummy Data) في ثوابت التطبيق المرفوعة يمثل خطراً على الصيانة (Maintainability)، لأنه في حال نسيان هذا التعليق وإطلاق التطبيق، سيجد مستخدمو الـ iOS رابطاً مكسوراً لا يعمل.

**الحل الأفضل:**
وضع الرابط الحقيقي إذا كان متوفراً، أو التعامل مع التطبيق بحيث لا يظهر الرابط لمستخدمي آبل مؤقتاً لحين استخراجه، أو سحب الروابط جميعها من إعدادات بعيدة (Remote Config).

---

## 6. تكرار غير مبرر يخل بالبساطة (Redundancy & Simplicity)
**الملف:** `lib/core/error/failure.dart`

**كيف تم كسر المبدأ؟**
تم استخدام ميزة `sealed class` بشكل صحيح لتمثيل الأخطاء. ولكن وجود دوال `factory constructors` داخل الكلاس `Failure` لتقوم فقط بإرجاع الكلاسات الوارثة (مثل `Failure.server` ترجع `ServerFailure`) هو أمر يضيف تعقيداً غير مبرر (Boilerplate) ولا يقدم فائدة حقيقية، مما يكسر معيار البساطة (Simplicity - Module 14).

**الحل الأفضل:**
حذف دوال الـ `factory constructors` بالكامل، والاعتماد على الكلاسات الموروثة مباشرة عند الإنشاء، مثال:
```dart
// بدلاً من
return const Failure.server(message: "Error");

// نستخدم مباشرة
return const ServerFailure(message: "Error");
```
هذا يقلل من حجم الكود ويجعله أكثر وضوحاً وسهولة في الصيانة.
